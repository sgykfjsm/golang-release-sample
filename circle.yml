machine:
  timezone:
    Asia/Tokyo
  post:
    - service mysql stop
    - service postgresql stop

dependencies:
  override:
    - make deps
    - make deps-release

test:
  override:
    - make test

deployment:
  release:
    branch: master
    commands:
      - make dist
      - make clean
